# llm-gateway
A FastAPI gateway that proxies chat requests to a local llama.cpp server, with health checks, Prometheus metrics, and Server-Sent Events streaming. A learning scaffold for production Python patterns in LLM serving — async I/O, structured logging, Pydantic config, and HTTP-mocked testing.
